{
  "name": "willmyflightbelate",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "build": "cd client && npm i --legacy-peer-deps && NODE_ENV=production npm run build && cd ../server && npm install",
    "start": "node server/index.js"
  }
}
